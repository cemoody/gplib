import setuptools


setuptools.setup(name='gplib',
                 version='0.1.0',
                 description='Gaussian Process Library',
                 long_description=open('README.md').read().strip(),
                 py_modules=['gplib'],
                 install_requires=[],
                 zip_safe=False)
